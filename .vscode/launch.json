{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "[Debug]",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/server/server.go",
            "env": {
                "PORT": "8080"
            },
            "args": []
        }
    ]
}